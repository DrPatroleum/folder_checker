# 📂 Folder Checker

Simple desktop tool that scans a selected folder and shows:
- number of files by extension,
- total disk usage,
- clean sortable table view.

Good for quick storage audits and file-type breakdowns.

## 🚀 Features
- Recursively scans all subfolders.
- Groups files by extension.
- Human-readable sizes (B, KB, MB, GB).
- Lightweight GUI using `tkinter`.
